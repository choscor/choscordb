cmake_minimum_required(VERSION 3.28)
project(ChoscorDB VERSION 0.1.0 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)
include(CTest)
include(cmake/Rust.cmake)
include(cmake/QualityGates.cmake)
find_package(Qt6 6.8 REQUIRED COMPONENTS Widgets Test Concurrent Svg)
include(cmake/MacOSQtCompatibility.cmake)
choscordb_remove_unavailable_agl()
find_path(QSCINTILLA_INCLUDE_DIR Qsci/qsciscintilla.h REQUIRED)
find_library(QSCINTILLA_LIBRARY NAMES qscintilla2_qt6 qscintilla2-qt6 REQUIRED)
choscordb_add_library(choscordb-widgets desktop/resources/resources.qrc desktop/design_system/theme.cpp desktop/design_system/theme_manager.cpp desktop/design_system/icons.cpp desktop/app/appearance_controller.cpp desktop/widgets/toast_region.cpp desktop/app/query_settings.cpp desktop/widgets/query_settings_dialog.cpp desktop/bridge/template_service.cpp desktop/widgets/editor_completion.cpp desktop/bridge/completion_service.cpp desktop/bridge/result_column_adapter.cpp desktop/app/editor_preferences.cpp desktop/widgets/preferences_dialog.cpp desktop/models/shortcut_catalog.cpp desktop/widgets/search_panel.cpp desktop/widgets/history_dock.cpp desktop/models/history_model.cpp desktop/widgets/sql_editor.cpp desktop/widgets/document_io.cpp desktop/app/main_window.cpp desktop/app/workspace_recovery.cpp desktop/models/result_table_model.cpp desktop/bridge/engine_adapter.cpp desktop/app/query_workspace.cpp desktop/models/navigator_model.cpp desktop/app/navigator_controller.cpp desktop/models/value_preview_model.cpp desktop/widgets/value_detail_dialog.cpp desktop/widgets/export_dialog.cpp desktop/widgets/profile_dialog.cpp)
target_include_directories(choscordb-widgets PUBLIC desktop)
target_sources(choscordb-widgets PRIVATE desktop/design_system/typography.cpp desktop/design_system/modal_panel.cpp desktop/design_system/control_style.cpp desktop/design_system/components.cpp desktop/widgets/dialog_shell.cpp desktop/widgets/confirmation_dialog.cpp desktop/design_system/platform_accessibility.cpp)
_choscordb_register_first_party_target(choscordb-widgets desktop/design_system/typography.cpp desktop/design_system/modal_panel.cpp desktop/design_system/control_style.cpp desktop/design_system/components.cpp desktop/widgets/dialog_shell.cpp desktop/widgets/confirmation_dialog.cpp desktop/design_system/platform_accessibility.cpp)
target_include_directories(choscordb-widgets SYSTEM PUBLIC ${QSCINTILLA_INCLUDE_DIR})
target_link_libraries(choscordb-widgets PUBLIC Qt6::Widgets Qt6::Concurrent Qt6::Svg ${QSCINTILLA_LIBRARY} choscordb-rust)
if(UNIX AND NOT APPLE)
  find_package(Qt6 6.8 REQUIRED COMPONENTS DBus)
  target_link_libraries(choscordb-widgets PRIVATE Qt6::DBus)
endif()
if(WIN32)
  target_compile_definitions(choscordb-widgets PUBLIC QSCINTILLA_DLL)
endif()
if(APPLE)
  target_link_libraries(choscordb-widgets PRIVATE "-framework AppKit")
endif()
add_dependencies(choscordb-widgets cargo-build_choscordb_bridge)
choscordb_add_executable(choscordb WIN32 MACOSX_BUNDLE desktop/app/main.cpp)
target_link_libraries(choscordb PRIVATE choscordb-widgets)
set_target_properties(choscordb PROPERTIES MACOSX_BUNDLE_BUNDLE_NAME ChoscorDB MACOSX_BUNDLE_GUI_IDENTIFIER org.choscordb.desktop)
if(BUILD_TESTING)
  install(CODE "message(FATAL_ERROR \"Installation requires BUILD_TESTING=OFF to exclude the developer preview. Use the release preset.\")")
endif()
install(TARGETS choscordb BUNDLE DESTINATION . RUNTIME DESTINATION bin)
if(APPLE)
  install(FILES docs/design/shadcn-reference/LICENSE.md DESTINATION "$<TARGET_BUNDLE_DIR_NAME:choscordb>/Contents/Resources/licenses" RENAME LICENSE-SHADCN)
  install(FILES docs/design/shadcn-reference/manifest.json DESTINATION "$<TARGET_BUNDLE_DIR_NAME:choscordb>/Contents/Resources/licenses" RENAME SOURCE-SHADCN.json)
  install(FILES desktop/resources/fonts/OFL.txt desktop/resources/fonts/SOURCE-GEIST.json LICENSE desktop/resources/icons/LICENSE-LUCIDE desktop/resources/icons/SOURCE-LUCIDE.json DESTINATION "$<TARGET_BUNDLE_DIR_NAME:choscordb>/Contents/Resources/licenses")
  install(FILES desktop/resources/icons/app-mark.svg desktop/resources/icons/play.svg desktop/resources/icons/square.svg desktop/resources/icons/plus.svg DESTINATION "$<TARGET_BUNDLE_DIR_NAME:choscordb>/Contents/Resources/icons")
else()
  install(FILES docs/design/shadcn-reference/LICENSE.md DESTINATION share/licenses/choscordb RENAME LICENSE-SHADCN)
  install(FILES docs/design/shadcn-reference/manifest.json DESTINATION share/licenses/choscordb RENAME SOURCE-SHADCN.json)
  install(FILES desktop/resources/fonts/OFL.txt desktop/resources/fonts/SOURCE-GEIST.json LICENSE desktop/resources/icons/LICENSE-LUCIDE desktop/resources/icons/SOURCE-LUCIDE.json DESTINATION share/licenses/choscordb)
  install(FILES desktop/resources/icons/app-mark.svg desktop/resources/icons/play.svg desktop/resources/icons/square.svg desktop/resources/icons/plus.svg DESTINATION share/choscordb/icons)
endif()
option(CHOSCORDB_BUILD_PERFORMANCE_PROBE "Build the standalone desktop performance probe" OFF)
if(CHOSCORDB_BUILD_PERFORMANCE_PROBE)
  choscordb_add_executable(choscordb-desktop-probe tests/performance/desktop_probe.cpp tests/performance/process_memory.cpp)
  target_link_libraries(choscordb-desktop-probe PRIVATE choscordb-widgets)
  if(WIN32)
    target_link_libraries(choscordb-desktop-probe PRIVATE psapi)
  endif()
endif()

option(CHOSCORDB_ENABLE_STAGING "Add the explicit unsigned application staging target" OFF)
if(CHOSCORDB_ENABLE_STAGING)
  include(cmake/Deploy.cmake)
endif()

if(BUILD_TESTING)
  choscordb_add_executable(choscordb-secondary-design-tests tests/desktop/secondary_design_test.cpp)
  target_link_libraries(choscordb-secondary-design-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME secondary-design COMMAND choscordb-secondary-design-tests)
  set_tests_properties(secondary-design PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  target_sources(choscordb-widgets PRIVATE desktop/design_system/preview_window.cpp)
  _choscordb_register_first_party_target(choscordb-widgets desktop/design_system/preview_window.cpp)
  target_compile_definitions(choscordb-widgets PRIVATE CHOSCORDB_DEVELOPMENT_PREVIEW)
  choscordb_add_executable(choscordb-typography-tests tests/desktop/typography_test.cpp)
  target_link_libraries(choscordb-typography-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME typography COMMAND choscordb-typography-tests)
  set_tests_properties(typography PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-icons-tests tests/desktop/icons_test.cpp)
  target_link_libraries(choscordb-icons-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME icons COMMAND choscordb-icons-tests)
  set_tests_properties(icons PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-modal-panel-tests tests/desktop/modal_panel_test.cpp)
  target_link_libraries(choscordb-modal-panel-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME modal-panel COMMAND choscordb-modal-panel-tests)
  set_tests_properties(modal-panel PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-control-style-tests tests/desktop/control_style_test.cpp)
  target_link_libraries(choscordb-control-style-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME control-style COMMAND choscordb-control-style-tests)
  set_tests_properties(control-style PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-components-tests tests/desktop/components_test.cpp)
  target_link_libraries(choscordb-components-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME components COMMAND choscordb-components-tests)
  set_tests_properties(components PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-preview-tests tests/desktop/preview_test.cpp)
  target_link_libraries(choscordb-preview-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME preview COMMAND choscordb-preview-tests)
  set_tests_properties(preview PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-appearance-adapter-tests tests/desktop/appearance_adapter_test.cpp)
  target_link_libraries(choscordb-appearance-adapter-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME appearance-adapter COMMAND choscordb-appearance-adapter-tests)
  set_tests_properties(appearance-adapter PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-component-gallery desktop/tools/component_gallery.cpp)
  target_link_libraries(choscordb-component-gallery PRIVATE choscordb-widgets)
  add_dependencies(choscordb-preview-tests choscordb-component-gallery)
  choscordb_add_executable(choscordb-design-system-tests tests/desktop/design_system_test.cpp)
  target_link_libraries(choscordb-design-system-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME design-system COMMAND choscordb-design-system-tests)
  set_tests_properties(design-system PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-modern-ui-tests tests/desktop/modern_ui_test.cpp)
  target_link_libraries(choscordb-modern-ui-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME modern-ui COMMAND choscordb-modern-ui-tests)
  set_tests_properties(modern-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-result-copy-workspace-tests tests/desktop/result_copy_workspace_test.cpp)
  target_link_libraries(choscordb-result-copy-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME result-copy-workspace COMMAND choscordb-result-copy-workspace-tests)
  set_tests_properties(result-copy-workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-query-settings-corruption-tests tests/desktop/query_settings_corruption_test.cpp)
  target_link_libraries(choscordb-query-settings-corruption-tests PRIVATE choscordb-widgets Qt6::Test)
  find_package(Python3 QUIET COMPONENTS Interpreter)
  if(Python3_Interpreter_FOUND)
    add_test(NAME ui-source-policy COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/scripts/ci/ui_policy.py)
    add_test(NAME query-settings-corruption COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/scripts/integration/query_settings_fixture.py $<TARGET_FILE:choscordb-query-settings-corruption-tests>)
    set_tests_properties(query-settings-corruption PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  endif()
  choscordb_add_executable(choscordb-query-settings-workspace-tests tests/desktop/query_settings_workspace_test.cpp)
  target_link_libraries(choscordb-query-settings-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME query-settings-workspace COMMAND choscordb-query-settings-workspace-tests)
  set_tests_properties(query-settings-workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-query-settings-controller-tests tests/desktop/query_settings_controller_test.cpp)
  target_link_libraries(choscordb-query-settings-controller-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME query-settings-controller COMMAND choscordb-query-settings-controller-tests)
  set_tests_properties(query-settings-controller PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-query-options-tests tests/desktop/query_options_test.cpp)
  target_link_libraries(choscordb-query-options-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME query-options COMMAND choscordb-query-options-tests)
  set_tests_properties(query-options PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-query-settings-dialog-tests tests/desktop/query_settings_dialog_test.cpp)
  target_link_libraries(choscordb-query-settings-dialog-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME query-settings-dialog COMMAND choscordb-query-settings-dialog-tests)
  set_tests_properties(query-settings-dialog PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-disconnect-workspace-tests tests/desktop/disconnect_workspace_test.cpp)
  target_link_libraries(choscordb-disconnect-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME disconnect-workspace COMMAND choscordb-disconnect-workspace-tests)
  set_tests_properties(disconnect-workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-navigator-sql-workspace-tests tests/desktop/navigator_sql_workspace_test.cpp)
  target_link_libraries(choscordb-navigator-sql-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME navigator-sql-workspace COMMAND choscordb-navigator-sql-workspace-tests)
  set_tests_properties(navigator-sql-workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-navigator-sql-tests tests/desktop/navigator_sql_test.cpp)
  target_link_libraries(choscordb-navigator-sql-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME navigator-sql COMMAND choscordb-navigator-sql-tests)
  set_tests_properties(navigator-sql PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-editor-completion-lifecycle-tests tests/desktop/editor_completion_lifecycle_test.cpp)
  target_link_libraries(choscordb-editor-completion-lifecycle-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME editor-completion-lifecycle COMMAND choscordb-editor-completion-lifecycle-tests)
  set_tests_properties(editor-completion-lifecycle PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-editor-completion-tests tests/desktop/editor_completion_test.cpp)
  target_link_libraries(choscordb-editor-completion-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME editor-completion COMMAND choscordb-editor-completion-tests)
  set_tests_properties(editor-completion PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-completion-service-tests tests/desktop/completion_service_test.cpp)
  target_link_libraries(choscordb-completion-service-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME completion-service COMMAND choscordb-completion-service-tests)
  set_tests_properties(completion-service PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-preferences-workspace-tests tests/desktop/preferences_workspace_test.cpp)
  target_link_libraries(choscordb-preferences-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME preferences-workspace COMMAND choscordb-preferences-workspace-tests)
  set_tests_properties(preferences-workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-preferences-tests tests/desktop/preferences_test.cpp)
  target_link_libraries(choscordb-preferences-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME preferences COMMAND choscordb-preferences-tests)
  set_tests_properties(preferences PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-search-tests tests/desktop/search_test.cpp)
  target_link_libraries(choscordb-search-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME search COMMAND choscordb-search-tests)
  set_tests_properties(search PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-history-tests tests/desktop/history_test.cpp)
  target_link_libraries(choscordb-history-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME history COMMAND choscordb-history-tests)
  set_tests_properties(history PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-value-preview-model-tests tests/desktop/value_preview_model_test.cpp)
  target_link_libraries(choscordb-value-preview-model-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME value-preview-model COMMAND choscordb-value-preview-model-tests)
  set_tests_properties(value-preview-model PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-recovery-tests tests/desktop/recovery_test.cpp)
  target_link_libraries(choscordb-recovery-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME recovery COMMAND choscordb-recovery-tests)
  set_tests_properties(recovery PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-workspace-tests tests/desktop/workspace_test.cpp)
  target_link_libraries(choscordb-workspace-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME workspace COMMAND choscordb-workspace-tests)
  set_tests_properties(workspace PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-navigator-model-tests tests/desktop/navigator_model_test.cpp)
  target_link_libraries(choscordb-navigator-model-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME navigator-model COMMAND choscordb-navigator-model-tests)
  set_tests_properties(navigator-model PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-result-model-tests tests/desktop/result_model_test.cpp)
  target_link_libraries(choscordb-result-model-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME result-model COMMAND choscordb-result-model-tests)
  set_tests_properties(result-model PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
  choscordb_add_executable(choscordb-editor-tests tests/desktop/editor_test.cpp)
  target_link_libraries(choscordb-editor-tests PRIVATE choscordb-widgets Qt6::Test)
  add_test(NAME editor COMMAND choscordb-editor-tests)
  set_tests_properties(editor PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
endif()

choscordb_add_quality_targets(choscordb-widgets)
