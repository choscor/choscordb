---
name: release-new-version
description: Release ChoscorDB end to end with reviewed release notes, quality checks, signed macOS packaging, an exact-commit Git tag, and verified GitHub Release publication. Also resume an already built release from its verified manifest.
---

# Release ChoscorDB

Work from the repository root. Read [the release guide](../../../docs/MACOS_RELEASE.md),
[the contribution checks](../../../CONTRIBUTING.md), and the current changelog.
Use this repository's Python release tools; packaging does not create tags.

## Scope and local configuration

Determine the requested version and whether the user wants preparation only,
upload of an existing build, or the full release. A request to release end to end
authorizes the release-specific commit, upload, tag push, and GitHub release;
carry it through without repeatedly requesting the same permission. An upload-only
request does not authorize tags or a GitHub release. Creating this skill does not
authorize running it. If scope is missing, prepare the notes and verified artifacts
before asking for the remaining publication decision.

Use the checked-out repository and its configured Git remote; never embed a
maintainer's checkout path, username, email, account ID, or private repository URL
in this skill. Resolve tools through PATH and existing local authentication.
Credentials belong in Keychain, tool-managed user configuration, or environment
variables, outside Git. Do not copy configuration from another application.
Use the dedicated `com.choscor.ChoscorDB` Sparkle key account.

## Prepare a new release or resume an existing one

For a new release, require stable `X.Y.Z` with no leading zeros. If no version was
provided, use an unambiguous version from the selected manifest or workspace;
otherwise ask. Inspect Git status and the selected remote branch. Fetch that remote
and check for divergence before release mutations; do not reset, stash, discard,
or commit unrelated work. Resolve a genuinely ambiguous destination with the user.

Inspect both local and remote `vX.Y.Z` tags and any existing GitHub release.
Treat network/authentication failures as errors, not missing tags or releases.
An existing tag is reusable only when its peeled commit equals the selected source
commit. Never force-push or move a conflicting tag.

For a new build, update `workspace.package.version` in Cargo.toml and run
`cargo check --workspace` to refresh Cargo.lock. Draft the matching `## [X.Y.Z]`
CHANGELOG.md section from actual changes since the previous applicable release
tag. Use concise user-facing descriptions; verify claims against the diff. Do not
paste raw commit bodies, private issues, database examples, or internal notes.
Show the draft while continuing checks, incorporating any edits the user provides.

Run `python scripts/ci/quality.py full` with the dependencies from the release
guide. Resolve failures before packaging; do not weaken checks. Review the diff
and stage only the intended version, lockfile, changelog, and release fixes using
explicit paths. For an authorized full release, commit those changes so packaging
captures a clean commit. Otherwise prepare the changes for the user's decision.

For an existing build, select its manifest explicitly. Do not change its version,
rebuild it, or tag current HEAD by assumption. Use its `source_commit`, inspect
that commit's changelog, and establish quality evidence for that exact commit.
Newer uncommitted tooling changes must not become the release's source identity.

## Package, verify, and inspect public content

Use `python scripts/release/macos.py prepare` when dependencies are not prepared,
then `preflight`, then `package --output build/releases/X.Y.Z` for a new build.
Use a fresh ignored output directory for an interrupted build. Never bypass
signing or notarization. Verify both new and existing builds with:

```sh
python scripts/release/macos.py verify --manifest "$CHOSCORDB_RELEASE_MANIFEST"
```

Set `CHOSCORDB_RELEASE_MANIFEST` locally to the selected manifest. Pass
`--sparkle-tools` only if the default dependency directory is not appropriate.
Do not write local tool paths into public metadata.

The public GitHub asset allowlist is exactly `ChoscorDB-X.Y.Z.dmg`,
`ChoscorDB.dmg`, and `choscordb-appcast.xml`. Keep source archives, checksums, manifests, SBOMs,
and other metadata local. Do not recursively upload the output directory or
use a release-directory wildcard. Build directories, staged app trees,
notarization submissions, logs, Cargo metadata, and tool configuration are not
release attachments. Keep full local verification and the source/license
materials generated by packaging; changing the upload set does not remove
bundled notices or substitute for checking dependency redistribution terms.

The verifier checks embedded local build paths; it is not a general secret
scanner. Also inspect proposed tracked changes, notes, the appcast, and bundled
content for credentials, personal local paths, private
endpoints, real connection data, and copied maintainer configuration. If a leak is
found, fix the source and rebuild/reverify rather than editing signed artifacts
or silently changing their checksums. Do not print suspected secret values.
Retain legitimate upstream license attribution and public signing metadata;
Developer ID signatures necessarily identify their signer. Do not promise an
anonymous signed app or claim that a pattern scan proves absence of every secret.

Check the rollout evidence required by the release guide, including clean macOS
26.0 installation and the signed update rehearsal. Record pending evidence
honestly; do not invent a pass or silently waive a required release check.

Prepare public release notes in an ignored file under `build/` from the changelog
at `source_commit`, with platform requirements, relevant known limitations, and
a link to the versioned GitHub DMG. A verified DMG SHA-256 can be included as text;
do not link to unpublished local checksum files. Never use raw local logs as notes.

## Tag the verified source commit

After local verification, use the manifest's full `source_commit` as
`CHOSCORDB_RELEASE_COMMIT` and `vX.Y.Z` as `CHOSCORDB_RELEASE_TAG`. Check local
and remote tags again immediately before mutation. Create an annotated tag only
if absent, targeting that exact commit:

```sh
git tag -a "$CHOSCORDB_RELEASE_TAG" "$CHOSCORDB_RELEASE_COMMIT" -m "ChoscorDB $CHOSCORDB_RELEASE_TAG"
git push "$CHOSCORDB_RELEASE_REMOTE" "refs/tags/$CHOSCORDB_RELEASE_TAG:refs/tags/$CHOSCORDB_RELEASE_TAG"
```

Reuse an identical existing tag. Do not push unrelated branches or all tags.
Verify the remote tag's peeled commit before publishing. Resolve the repository
from the manifest's GitHub releases base URL and require it to match the selected
Git remote; do not expose credentials embedded in a remote URL. The publisher
requires the remote tag to resolve to `source_commit` and never creates tags.

## Publish on GitHub

Use `gh` with existing local authentication and `curl` for public verification.
GitHub Releases is the only publication destination. The manifest's `base_url`
is `https://github.com/OWNER/REPO/releases`. The app embeds the stable feed URL
`BASE/latest/download/choscordb-appcast.xml`; enclosures use
`BASE/download/vX.Y.Z/ChoscorDB-X.Y.Z.dmg`. Forks select their repository at
packaging time with `--base-url`; never rewrite a signed app or its manifest to
change the host afterward.

Run the checked-in publisher rather than assembling independent upload commands:

```sh
python scripts/release/publish.py --manifest "$CHOSCORDB_RELEASE_MANIFEST" --notes-file "$CHOSCORDB_RELEASE_NOTES" --dry-run
python scripts/release/publish.py --manifest "$CHOSCORDB_RELEASE_MANIFEST" --notes-file "$CHOSCORDB_RELEASE_NOTES"
```

Set `CHOSCORDB_RELEASE_NOTES` to the reviewed ignored notes file. The dry-run
reads remote state and validates the selected release without publishing. The
publisher creates or resumes a draft, uploads the versioned DMG first, identical
`ChoscorDB.dmg` next, and appcast last. It verifies each uploaded asset before
proceeding and publishes the complete draft as latest only after validation.
The public versioned downloads and stable latest feed are then checked. Never
publish an empty draft, use a wildcard attachment path, or use `--clobber` to
repair a conflicting asset.

Each release has its own signed, single-release appcast; historical releases and
their assets remain available under their tags. Do not merge appcasts from the
previous hosting setup. Refuse a stale version that would replace a newer stable
release as latest. Authentication or network failures are errors, not evidence of
absence. Assume one maintainer publishes at a time across machines; the command
also prevents overlapping local publication.

On retry, reuse matching draft assets and upload only missing files. Conflicting
bytes, notes, or tag targets require a deliberate decision; never overwrite or
move them implicitly. If verification fails after publication, report that the
release is already public and retry verification without rebuilding or deleting
it. A faulty shipped version should normally be fixed with a higher version.

The original 0.1.0 build embeds the old hosting URL. Its explicitly requested
replacement uses the GitHub feed, but existing installations cannot change
automatically: users of the original build must manually install the replacement.
For future releases, increase the version. Do not publish to the former host or
claim automatic migration of the original build.

## Report

Report the version and exact source commit, checks and pending rollout evidence,
GitHub download and feed links, tag push result, and GitHub release link or draft
status. Distinguish published steps from incomplete steps. Never include local
credential identifiers, private paths, or raw provider diagnostics in release notes.
