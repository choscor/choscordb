[package]
name = "choscordb-postgres-values"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
choscordb-driver-api = { path = "../driver-api" }
