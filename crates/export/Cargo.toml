[package]
name = "choscordb-export"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
[lints]
workspace = true
[dependencies]
choscordb-driver-api = { path = "../driver-api" }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["fs", "io-util", "sync", "rt", "macros"] }
tempfile = "3"

[dev-dependencies]
proptest = "1"
csv = "1"
